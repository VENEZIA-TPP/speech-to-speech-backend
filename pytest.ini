[pytest]
asyncio_mode = auto
pythonpath = .
testpaths = tests
# A hung WebSocket test is worse than a failing one: it blocks forever without
# naming itself. Generous enough that no green test is near it - the whole
# suite runs in about 10 s.
timeout = 60
